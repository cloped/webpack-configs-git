# Some anotations of html loader config webpack

- `html-loader`: used to transform html in text
- `html-webpack-plugin`: used to create the files html in the dist
- `file-loader`: used to put images in dist

